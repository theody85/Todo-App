function calculator(valueOne, operator, valueTwo) {
  let results;

  if (operator === "+") {
    results = valueOne + valueTwo;
  } else if (operator === "-") {
    results = valueOne - valueTwo;
  }
}

// const calculator = () => {

// }
